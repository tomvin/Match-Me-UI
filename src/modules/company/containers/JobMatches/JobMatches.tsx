import React from 'react'
import './JobMatches.scss';

interface Props {
  jobId: string;
}

const JobMatches = (props: Props) => {
  return <div>Query for complete matches for this job not implemented yet. </div>
}

export default JobMatches
